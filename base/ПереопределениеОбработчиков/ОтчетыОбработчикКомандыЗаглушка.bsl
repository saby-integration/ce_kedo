		Символов10 = СтрДлина(ПрефиксПродукта() + "КЭДО_");
		Если Врег(Лев(Команда.Имя, Символов10)) = Врег(ПрефиксПродукта() + "КЭДО_") Тогда
			Saby_ОбработчикКоманды_КЭДО(ФормаОтчета, Команда, Результат, context_params);
		КонецЕсли;
