	#Если ВнешнееСоединение Тогда
		// Выход при COM соединении
	#Иначе
		// Только обработка локальных вызовов
		Saby_Core.Saby_ЗаписьДокументаСоСреднимЗаработкомПередЗаписью(
			Источник,
			Отказ,
			РежимЗаписи,
			РежимПроведения);
	#КонецЕсли  	
